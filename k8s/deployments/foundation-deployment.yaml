# Foundation Service Deployment (统一 ERP 服务)
# 合并了所有微服务：基础服务、服务管理、订单工作流、数据分析监控
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-foundation-service
  namespace: default
  labels:
    app: crm-foundation-service
    service: foundation
    version: python
    environment: development
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crm-foundation-service
  template:
    metadata:
      labels:
        app: crm-foundation-service
        service: foundation
        version: python
        environment: development
    spec:
      # 确保 Pod 使用节点的 IP 作为出口 IP（K3s 默认已启用 SNAT）
      # 这样可以将节点 IP (168.231.118.179) 加入天眼查 API 白名单
      # 注意：hostNetwork 会改变网络行为，当前配置不需要它
      # Pod 的出口 IP 已经是节点 IP，通过 SNAT 实现
      containers:
      - name: foundation
        image: bantu-crm-crm-foundation-service:dev
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8081
          protocol: TCP
        # 开发模式：使用热重载启动命令
        command: ["uvicorn", "foundation_service.main:app", "--host", "0.0.0.0", "--port", "8081", "--reload", "--reload-dir", "/app", "--reload-include", "*.py", "--log-level", "debug"]
        env:
        # 从 MySQL ConfigMap 读取数据库配置（优先使用 mysql-config）
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: mysql-config
              key: DB_HOST
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: mysql-config
              key: DB_PORT
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: mysql-config
              key: DB_NAME
        # 从 MySQL Secret 读取敏感配置（优先使用 mysql-secret）
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_PASSWORD
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: crm-python-secret
              key: JWT_SECRET
        - name: JWT_ALGORITHM
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: FOUNDATION_JWT_ALGORITHM
        - name: JWT_EXPIRATION
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: FOUNDATION_JWT_EXPIRATION
        - name: CORS_ALLOWED_ORIGINS
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: CORS_ALLOWED_ORIGINS
        # 开发模式环境变量
        - name: PYTHONUNBUFFERED
          value: "1"
        # 开发模式：覆盖 DEBUG 为 true（如果 ConfigMap 中为 false）
        - name: DEBUG
          value: "true"
        # Redis 配置（用于缓存和监控）
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: REDIS_HOST
        - name: REDIS_PORT
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: REDIS_PORT
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: crm-python-secret
              key: REDIS_PASSWORD
        - name: REDIS_DB
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: REDIS_DB
        # MongoDB 配置（用于日志查询）
        - name: MONGO_HOST
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: MONGO_HOST
        - name: MONGO_PORT
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: MONGO_PORT
        - name: MONGO_DATABASE
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: MONGO_DATABASE
        - name: MONGO_USERNAME
          valueFrom:
            secretKeyRef:
              name: crm-python-secret
              key: MONGO_USERNAME
        - name: MONGO_PASSWORD
          valueFrom:
            secretKeyRef:
              name: crm-python-secret
              key: MONGO_PASSWORD
        - name: MONGO_AUTH_SOURCE
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: MONGO_AUTH_SOURCE
        # 天眼查 API 配置
        - name: TIANYANCHA_API_KEY
          valueFrom:
            secretKeyRef:
              name: crm-python-secret
              key: TIANYANCHA_API_KEY
        - name: TIANYANCHA_API_URL
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: TIANYANCHA_API_URL
        - name: TIANYANCHA_TIMEOUT
          valueFrom:
            configMapKeyRef:
              name: crm-python-config
              key: TIANYANCHA_TIMEOUT
        volumeMounts:
        # 挂载本地代码目录，实现热重载
        - name: common-code
          mountPath: /app/common
        - name: foundation-code
          mountPath: /app/foundation_service
        resources:
          # 开发环境：降低资源限制
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
      volumes:
      # 开发模式：挂载本地代码目录
      - name: common-code
        hostPath:
          path: /home/bantu/crm-backend-python/common
          type: Directory
      - name: foundation-code
        hostPath:
          path: /home/bantu/crm-backend-python/foundation_service
          type: Directory


