# BANTU CRM Python Backend - 依赖包

# FastAPI 核心
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 数据库
sqlalchemy==2.0.23
alembic==1.12.1
aiomysql==0.2.0  # 异步 MySQL 驱动
pymysql==1.1.0  # 同步 MySQL 驱动（用于 Alembic）
cryptography==41.0.7

# 数据验证
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.2.0  # Email 验证支持

# 认证授权
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# HTTP 客户端（服务间调用）
httpx==0.25.1

# 环境变量
python-dotenv==1.0.0

# 日志
loguru==0.7.2

# 工具
python-multipart==0.0.6  # 文件上传支持

# 开发工具
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
ruff==0.1.6

